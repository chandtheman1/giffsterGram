<div class="d-flex justify-content-center">
    <div class="row d-flex d-none d-lg-block my-5"><img src="/images/LogoCropped.png" width="auto" height="200" id="logo"></div>
</div>

    {{#each gifs as |gif|}}
    <div class="d-flex justify-content-center">
        <div class="row mt-1 align-self-center">
            <a href="/api/gif/{{id}}"> 
            {{!-- needs to be fixed --}}
                <img src="data:image/gif;base64,{{gif}}" alt="Alt text" />
            </a>
        </div>
    </div>
    {{/each}}

